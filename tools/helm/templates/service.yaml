apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: {{ .Values.appPortName }}
